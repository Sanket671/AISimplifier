services:
  - type: web
    name: legal-document-simplifier
    env: python
    plan: free
    buildCommand: |
      cd backend
      pip install -r requirements.txt
    startCommand: |
      cd backend
      gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0